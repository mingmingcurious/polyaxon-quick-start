version: 1.1
kind: operation
name: build
params:
  destination:
    connection: registry-dbydocker-repo
    value: polyaxon/polyaxon-quick-start:xw
runPatch:
  init:
  - dockerfile:
      image: "tensorflow/tensorflow:2.4.2"
      run:
      - 'pip3 install --no-cache-dir -U polyaxon'
      langEnv: 'en_US.UTF-8'
hubRef: kaniko